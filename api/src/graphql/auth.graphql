input LoginUser {
    username: String!
    password: String!
}

input NewUser {
    username: String!
    password: String!
    password_confirm: String!
}

type Query {
  login(user: LoginUser!): String!
  register(user: NewUser!): String!
}